<Window x:Class="DelegateTes.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DelegateTes"
        mc:Ignorable="d"
        Title="MainWindow" Height="100" Width="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="input" Grid.Row="0" Grid.Column="0" Margin="10" VerticalAlignment="Center" Text="あいうえお"/>
        <Button  Grid.Row="0"  Grid.Column="1" Margin="10" x:Name="button" Content="Click Me" VerticalAlignment="Center" Click="button_Click"/>
    </Grid>

 
</Window>
